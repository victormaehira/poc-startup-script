sudo mkdir -p /opt/projetos
cd /opt/projetos

sudo apt -y install git
sudo git clone https://github.com/victormaehira/poc-startup-script.git
cd poc-startup-script

#escolha dir = biozoomserver ou quality ou facecheck
export dir=quality

sudo sed -i 's/${SPRING_DATASOURCE_USERNAME}/<COLOQUE_USER_AQUI>/g' ./envlist-repo/*.txt
sudo sed -i 's/${SPRING_DATASOURCE_PASSWORD}/<COLOQUE_A_SENHA_AQUI>/g' ./envlist-repo/*.txt
sudo sed -i 's/${SPRING_DATASOURCE_IP}/<COLOQUE_O_IP_DO_DB_AQUI>/g' ./envlist-repo/*.txt

source .env

sudo apt -y install docker.io

sudo curl -L "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

sudo docker network create facecheck_network

sudo gcloud auth configure-docker
sudo cp ${dir}/docker-compose.yml ./
sudo docker-compose up -d